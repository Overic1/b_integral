{% extends "base.html.twig" %}

{% block title %}B-integral{% endblock %}

{% block body %}

<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="#">B-Integral</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    {% if app.user %}
        <ul class="navbar-nav mr-auto">
           <li class="nav-item active">
                <a class="nav-link" href="{{ path('security.logout') }}">Se deconnecter</span></a>
            </li> 
        </ul>
    {% endif %}
    
  </div>
</nav>

<div class="container">
    <div class="jumbotron mt-4">
        <div class="col-sm-6">
            <div class="card">
                <div class="card-body">
                    <a href='https://regie.myn2a.online/www/delivery/ck.php?n=a05aa9bf&amp;cb=INSERT_RANDOM_NUMBER_HERE' target='_blank'><img class=" img-fluid mb-2 mt-2" width="100%" height="100%" src='https://regie.myn2a.online/www/delivery/avw.php?zoneid=1&amp;cb=INSERT_RANDOM_NUMBER_HERE&amp;n=a05aa9bf' border='0' alt='' /></a>
                </div>
            </div>
        </div>
        {% if app.user %}
            {% if isAdmin %}
                <a class="btn btn-primary btn-lg" href="{{ path('admin') }}" role="button">Interface administrateur</a>    
            {% else %}
                <a class="btn btn-primary btn-lg" href="{{ path('index') }}" role="button">Voir le Dashbord</a>    
            {% endif %}
        {% else %}
            <a class="btn btn-primary btn-lg" href="{{ path('security.login') }}" role="button">Se connecter</a>
        {% endif %}        
    </div>
</div>

{% endblock %}
